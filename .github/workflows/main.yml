{
  "operation": "create",
  "language": "yaml",
  "prompt": "Crie um arquivo de workflow do GitHub Actions (deploy.yml) para um projeto Vite. O workflow deve:\n1. Rodar em cada push na branch 'main'.\n2. Usar Node.js 20.\n3. Instalar dependências (npm install).\n4. Rodar o build (npm run build).\n5. Fazer o upload do artefato da pasta 'dist'.\n6. Fazer o deploy para o GitHub Pages.\n\nCertifique-se de incluir as permissões necessárias para o GITHUB_TOKEN (contents: read, pages: write, id-token: write).",
  "shouldRecommendSkip": false
}
